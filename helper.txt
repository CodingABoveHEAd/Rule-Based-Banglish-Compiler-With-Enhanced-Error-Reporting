test command:

unalias flex bison
flex lexer.l
gcc lex.yy.c -o lexer
./lexer sample.bl > output.txt

important rules for better understanding:
([^\\\n]|\\.) any charecter except \ , \n or any other \t,\b rtc.
int len = (int)strlen(yytext) - 2; -2 because two quotes elimination
strncpy(yylval.sval, yytext + 1, len);  yytext + 1 → skips opening quote "


/* Resolve dangling-else: ELSE_KW binds tighter than "no else" */
%nonassoc LOWER_THAN_ELSE
%nonassoc ELSE_KW


Void variable declaration (shunno x; — makes no sense)
Break/continue outside loop (very common mistake)
Modulus with float operands (3.14 % 2 — invalid)
Return type mismatch (function says purno but does ferot;)
Function param count validation (calling add(1) when it takes 2)
Non-boolean condition warning (jodi (5) instead of jodi (5 > 0))